# ED115OC1 구동 계획

**Glider but Cheaper** (Caster EPDC / Gowin GW2A-LV18 포트)로 11.5″ 2760×2070 전자잉크 패널
**ED115OC1**을 구동하기 위한 설계 계획서.

목표는 두 단계다.

| 단계 | 내용 | DDR3 |
|-|-|-|
| **1단계** | FPGA가 만든 데모 영상으로 패널을 60 Hz 구동 | **불필요** |
| 2단계 | HDMI 입력을 받아 모니터로 동작 | 필요 |

1단계는 `make demo` 로 지금 바로 빌드된다([7장](#7-ddr3-없이-구동하기--1단계-목표)).

이 문서는 현재 코드 상태 분석, 설계 근거, 파일별 수정 목록, 단계별 브링업 절차를 담는다.
숫자는 모두 레포 안의 코드·스키매틱·문서에서 도출했고, 도출 과정은 부록에 남겼다.

---

## 목차

- [개요](#개요)
  - [목표 구성](#목표-구성)
  - [확정 사항](#확정-사항)
  - [남은 미확정 사항](#남은-미확정-사항)
  - [현재 코드 상태](#현재-코드-상태)
- [1. 대상 하드웨어](#1-대상-하드웨어)
  - [1.1 패널](#11-패널)
  - [1.2 40핀 커넥터 핀맵](#12-40핀-커넥터-핀맵)
  - [1.3 시스템 구성](#13-시스템-구성)
  - [1.4 EE03 보드 실물 구조](#14-ee03-보드-실물-구조)
  - [1.5 전원 레일](#15-전원-레일)
  - [1.6 FPGA 보드와 배선](#16-fpga-보드와-배선)
- [2. 왜 2배 확대인가](#2-왜-2배-확대인가)
  - [2.1 메모리 대역폭](#21-메모리-대역폭)
  - [2.2 처리 속도](#22-처리-속도)
  - [2.3 치르는 대가](#23-치르는-대가)
- [3. 목표 스펙](#3-목표-스펙)
  - [3.1 운용 해상도 선정](#31-운용-해상도-선정)
  - [3.2 타이밍 파라미터](#32-타이밍-파라미터)
  - [3.3 프레임레이트 여유](#33-프레임레이트-여유)
  - [3.4 스캔 타이밍과 입력 타이밍의 관계](#34-스캔-타이밍과-입력-타이밍의-관계)
- [4. 클럭 설계](#4-클럭-설계)
- [5. 출력단 재설계](#5-출력단-재설계)
  - [5.1 현재 구조의 문제](#51-현재-구조의-문제)
  - [5.2 2배 확대를 배선으로](#52-2배-확대를-배선으로)
  - [5.3 SDCLK 출력](#53-sdclk-출력)
- [6. DDR3 서브시스템](#6-ddr3-서브시스템)
  - [6.1 현재 상태](#61-현재-상태)
  - [6.2 Gowin DDR3 IP](#62-gowin-ddr3-ip)
  - [6.3 memif 어댑터](#63-memif-어댑터)
  - [6.4 비동기 FIFO 교체](#64-비동기-fifo-교체)
- [7. DDR3 없이 구동하기 — 1단계 목표](#7-ddr3-없이-구동하기--1단계-목표)
  - [7.1 왜 DDR3 없이 되는가](#71-왜-ddr3-없이-되는가)
  - [7.2 구동 방식](#72-구동-방식)
  - [7.3 빌드 타깃](#73-빌드-타깃)
  - [7.4 검증 3단계](#74-검증-3단계)
  - [7.5 디버그 가이드](#75-디버그-가이드)
  - [7.6 튜닝 노브](#76-튜닝-노브)
- [8. 영상 입력](#8-영상-입력)
- [9. 파일별 수정 목록](#9-파일별-수정-목록)
- [10. 마일스톤](#10-마일스톤)
- [11. 리스크](#11-리스크)
- [부록 A: 40핀 핀맵 복원 근거](#부록-a-40핀-핀맵-복원-근거)
- [부록 B: 계산 근거](#부록-b-계산-근거)
- [부록 C: Caster 기능 이식 감사](#부록-c-caster-기능-이식-감사)

---

## 개요

### 목표 구성

| 항목 | 값 |
|-|-|
| 패널 | ED115OC1, 11.5″, 2760×2070, V220 Pearl |
| SD 버스 | **16비트** (`OUTPUT_16B` 활성화) |
| 입력 / VRAM 격자 | **1280×960** (VESA 표준 모드) |
| 패널 스캔 영역 | **2560×1920** (2배 확대, 면적 86%) |
| 프레임레이트 | **60 Hz** (70 Hz 상향 여지 있음) |
| SDCLK = 코어 클럭 | **40.5 MHz** (rPLL 27 × 3/2, 60 Hz 필요 최소 39.8 MHz) |
| 프레임버퍼 | 2,457,600 B (2.34 MB) |
| DDR3 사용률 | 332 MB/s (천장 대비 30%) |
| DVI 입력 | 85 MP/s (한계 105 MP/s) |

### 확정 사항

- **SD 버스는 16비트다.** `pcb/40p-adapter-ab/adapter.kicad_sch`에서 40핀 핀맵 전체를 복원했다.
  `ED0`~`ED15` 16개 데이터선이 배선돼 있고, 마더보드 50핀의 `PD0`~`PD15`와 1:1로 대응한다.
  ([부록 A](#부록-a-40핀-핀맵-복원-근거))
- **풀해상도 구동은 불가능하다.** DDR3 대역폭과 처리 속도 양쪽에서 막힌다. ([2장](#2-왜-2배-확대인가))
- **Gowin DDR3 IP의 유저 인터페이스가 `memif.v`와 거의 그대로 맞는다.**
  커맨드 인코딩과 버스트 길이 규약이 동일하다. ([6.3](#63-memif-어댑터))
- **EPDC 코어는 100% 이식됐다.** 포트 `caster.v`의 인스턴스 목록이 업스트림과 동일하고
  8개 모드가 전부 살아 있다. 빠진 것은 영상 입력 체인과 호스트 제어다.
  ([부록 C](#부록-c-caster-기능-이식-감사))

### 완료된 RTL 작업

| 파일 | 내용 |
|-|-|
| `defines.vh` | `OUTPUT_16B` 활성화, `DEFAULT_HACT` 640→320, 블랭킹 조정 |
| `sysclock.v` | Gowin rPLL 40.5 MHz (27 × 3/2), 진짜 lock 신호 |
| `caster.v` | 16비트 2배 복제 출력단, ODDR SDCLK, GDCLK 원복, `sys_ready` 게이팅, 스캔 FSM 복원, `PANEL_TEST` 데모 모드 |
| `top.v` | 리셋 시퀀서, vsync 675,000 clk = 60.0 Hz, `sys_ready` 배선 |
| `bi_fifo.v` / `bo_fifo.v` | 그레이코드 비동기 FIFO로 전면 교체 |
| `vin.v` | `v_ready` 백프레셔 존중하는 pull 방식으로 재작성 |
| `epd_selftest.v` | **신규** — EPD 버스 자체 검증, LED D2 보고 |
| `debug_ctrl.v` | **신규** — 5버튼 디바운스, BTN0 안전 게이트, LED 표시 |
| `csr_master.v` | **신규** — MCU 없이 모드 전환 (BTN3) |
| `uart_tx.v` / `debug_uart.v` | **신규** — UART 디버그 콘솔 (M11) |
| `epd_sd_check.v` | **신규** — SD 버스: 16비트 실효·Hi-Z·가로 2배 배선 |
| `epd_line_dup.v` | **신규** — 세로 2배: 라인 쌍 CRC 서명 비교 |
| `Makefile` | `demo` / `bringup` 타깃 추가 |

작업 중 잡은 버그: `dbg_scan_state` 사용-후-선언, 블랭킹 축소로 생긴 GDCLK 여분 펄스
(프레임당 1928 → 1927), 세로 확대 라인버퍼의 1워드 어긋남.

### 남은 미확정 사항

**패널의 SDCLK 최대 주파수.** 이 값이 프레임레이트 천장을 직접 결정하며 FPGA 성능으로 우회할 수 없다.

#### 데이터시트 조사 결과 (2026-08-22) — **공개된 것이 없다**

| 소스 | 결과 |
|-|-|
| PanelLook (ED115OC1 overview 68751) | 캡차 차단, 스펙 요약뿐 PDF 없음 |
| Andesource(유통사) 제품 페이지 | **"Data sheet: Work in progress, stay tuned!"** — 유통사조차 미보유 |
| DatasheetArchive / Good Display / Waveshare | ED115 계열 없음 |
| eBay · AliExpress · Amazon | 판매 리스팅만, 스펙 없음 |

2012년 패널이고 E Ink가 일반 배포한 적이 없어, NDA 없이 입수될 가능성이 낮다.
**현재 확보 가능한 최대 정보는 루트 `README.md:1349` 한 줄이다.**

#### epdiy 저자의 "max 10 MHz" 주장과 그 반박

epdiy 저자 martinberlin이 Tindie 상품 페이지에 이렇게 적어 두었다:

> "Please avoid buying old Einks like **ED115OC1** (nice resolution but super huge buffer with **max 10 MHz clock**)"

이 계획은 40.5 MHz를 요구하므로, 사실이라면 4배 초과다. **그러나 이것은 패널 규격이 아니라
드라이버 플랫폼의 한계일 가능성이 매우 높다.** 근거 셋:

**1. epdiy의 `bus_speed`는 바이트레이트가 일정하다.**

| 패널 | 버스 | `bus_speed` | 바이트레이트 |
|-|-|-|-|
| ED097TC2 | 8비트 | 22 MHz | 22.0 MB/s |
| ED052TC4 | 8비트 | 22 MHz | 22.0 MB/s |
| ED060SCT | 8비트 | 20 MHz | 20.0 MB/s |
| ED133UT2 | 8비트 | 20 MHz | 20.0 MB/s |
| ED078KC1 | 16비트 | 11 MHz | 22.0 MB/s |
| ED103MC2 | 16비트 | 11 MHz | 22.0 MB/s |

바이트레이트가 20~22 MB/s로 고정이다. 16비트 패널이 정확히 절반 클럭인 것은 패널 특성이 아니라
**ESP32 LCD 주변장치의 처리량 한계**다. "10 MHz"도 같은 ESP32측 관측치로 보는 것이 자연스럽다.

**2. 저자 본인이 "동작한다"고 말한다.** epdiy Discussion #390:

> "I also tested today 11.5 Inch ED115oc1 and **works** but for some reason I still didn't found
> epdiy driver is hanging when the width is > 2000 pixels."

미지원 사유로 실제 거론되는 것은 (a) width > 2000 시 드라이버 행(Issue #391 계열),
(b) PSRAM 프레임버퍼 2 MB 한계 — **둘 다 ESP32 플랫폼 문제이지 클럭 상한이 아니다.**

**3. 플랫폼 성격이 다르다.** epdiy는 ESP32 기반 저주사율 부분갱신 드라이버이고,
Caster는 FPGA 기반 고주사율 연속 스캔 아키텍처다. **드라이버측 병목을
패널측 제약으로 오귀속한 것**으로 보는 편이 정합적이다.
루트 README가 이 패널을 **Tested: Yes**로 분류하는 것도 이 해석을 지지한다.

또한 "super huge buffer"는 폭 2760픽셀 = 소스드라이버 시프트레지스터가 길다는 뜻으로,
**라인당 로드 시간**이 길다는 얘기이지 클럭 상한과는 다른 축의 문제다.

> ⚠️ 다만 이 반박은 **정황 근거**다. 반증은 아니다. 실측 전까지 40.5 MHz는 미검증 가정으로 남는다.

#### 실측 전략 (데이터시트 없이 상한을 찾는 법)

`sysclock.v`의 rPLL 분주비만 바꿔 비트스트림 몇 개를 굽고, 화면이 깨지는 지점을 찾는다.

| 목표 SDCLK | rPLL (27 MHz 입력) | 가능 프레임레이트 |
|-|-|-|
| 13.5 MHz | ×1/2 | 20.4 Hz |
| 20.25 MHz | ×3/4 | 30.5 Hz |
| 27 MHz | ×1 (바이패스) | 40.7 Hz |
| 33.75 MHz | ×5/4 | 50.9 Hz |
| **40.5 MHz** | **×3/2 (현재)** | **61.1 Hz** |

낮은 쪽부터 올린다. 40.5 MHz가 불가하면 **프레임레이트를 낮춰 타협 가능**하다 —
프레임당 스캔 클럭이 662,888이므로 필요 SDCLK = 662,888 × 목표 FR.
30 Hz면 19.9 MHz로 충분하다. 즉 최악의 경우에도 프로젝트가 무산되지는 않는다.

#### 아직 시도하지 않은 입수 경로

1. **zephray(Modos)에게 직접 문의** — README에 Tested: Yes를 적은 당사자이므로
   실제 구동 클럭을 알고 있을 것이다. **승산이 가장 높다.**
2. 패널 원출처 기기 역추적 — 2012년 11.5″ 300 ppi. 해당 기기 서비스 매뉴얼에 타이밍이 있을 수 있다. 미특정.
3. AliExpress/eBay 셀러에게 스펙 시트 요청.
4. Andesource RFQ (`andehk@andesource.com`) — 유통사라 요약본은 줄 여지가 있다.

이 계획은 60 Hz 기준 **40.5 MHz**를 요구한다.

### 현재 코드 상태

`Glider_but_cheaper`는 **Caster의 EPDC 코어를 Gowin으로 포팅한 결과물이 맞다.**
다만 플랫폼 레이어가 비어 있다.

**포팅이 끝난 부분** — 업스트림 `Caster/rtl/` 대비:

| 파일 | 차이 |
|-|-|
| `degamma.v` `bayer_dithering.v` `blue_noise_dithering.v` `line_reverse.v` `adder_sat.v` `adder_sat_8.v` | 0줄 |
| `pixel_processing.v` | 27줄 (wire 선언 위치 이동, 로직 동일) |
| `wvfmlut.v` / `csr.v` / `defines.vh` | 2 / 5 / 2줄 |
| **`memif.v`** | **0줄 — 완전 동일** |
| `caster.v` | 183줄 (2배 확대 추가, free-run 개조, GDCLK 개조) |

Xilinx 종속 프리미티브(`ODDR2`, `chipscope_*`)가 제거돼 있고 yosys + nextpnr로 합성이 통과한다.

**비어 있는 부분** — 업스트림 `Caster/rtl/spartan6/` 대비:

| 파일 | 업스트림 | 현재 |
|-|-|-|
| `mig_wrapper.v` | 139줄, `s6_ddr3` (Xilinx MCB) | 92줄, **128비트 레지스터 1개 루프백** |
| `sysclock.v` | 107줄, DCM/PLL | 20줄, **`assign clk_ddr = clk_in;`** |
| `vin.v` | 191줄, DPI/DVI/FPD-Link 입력 | 100줄, **내부 테스트 패턴** |
| `top.v` | 538줄 | 620줄 (152줄 차이) |

현재 `defines.vh`가 만드는 실효값:

```
                      작업 전                  현재
입력(= VRAM 격자)   1280 x 960              1280 x 960
패널 스캔           2560 x 1920 (8비트)     2560 x 1920 (16비트)
htotal / vtotal     680 / 1942              344 / 1927
코어 = SDCLK        27.0 MHz (PLL 없음)     40.5 MHz (rPLL)
프레임레이트        20.4 Hz                 60.0 Hz
FBYTES              2,457,600 B             2,457,600 B
```

우측 200 px, 하단 150줄이 미스캔인 것은 2배 확대의 결과로 그대로다([2.3](#23-치르는-대가)).

> **왜 8비트인가**
> 설계 판단이 아니다. 포트의 `defines.vh`는 업스트림과 **`CSR_SELFBOOT` 한 줄만** 다르고,
> `OUTPUT_16B`(73번 줄)는 업스트림 그대로 주석 처리된 상태다. 건드린 적이 없다.
> 업스트림 기본값이 8비트인 것은 Modos 기본 키트(6″·13.3″)가 8비트 패널이기 때문이고,
> 루트 README도 "16비트 패널을 쓸 때 이 플래그를 정의하라"고 안내한다.

---

## 1. 대상 하드웨어

### 1.1 패널

루트 README의 스크린 목록 기준:

| 항목 | 값 |
|-|-|
| 모델 | ED115OC1 |
| SN Initial | E4R |
| FPL 플랫폼 | V220 (Pearl) |
| 해상도 | 2760×2070 |
| 반사율 / 명암비 | 35% / 12:1 |
| 출시 | 2012 |
| 인터페이스 | TTL, 40핀 |
| 어댑터 | 40P-A |
| 검증 | Yes |

네이티브 화소 밀도는 약 300 PPI다.

### 1.2 40핀 커넥터 핀맵

| 핀 | 신호 | 핀 | 신호 |
|-:|-|-:|-|
| 1 | VGL (−20V) | 21 | ED7 |
| 2 | NC | 22 | GND |
| 3 | VGH (+22V) | 23 | ED8 |
| 4 | NC | 24 | ED9 |
| 5 | +3V3 | 25 | ED10 |
| 6 | GDOE | 26 | ED11 |
| 7 | GDCLK | 27 | ED12 |
| 8 | GDSP | 28 | ED13 |
| 9 | GND | 29 | ED14 |
| 10 | VCOM | 30 | ED15 |
| 11 | +3V3 | 31 | SDCE0 |
| 12 | GND | 32 | SDLE |
| 13 | SDCLK | 33 | SDOE |
| 14 | ED0 | 34 | NC |
| 15 | ED1 | 35 | NC |
| 16 | ED2 | 36 | VPOS (+15V) |
| 17 | ED3 | 37 | NC |
| 18 | ED4 | 38 | VNEG (−15V) |
| 19 | ED5 | 39 | NC |
| 20 | ED6 | 40 | BORDER |

마운팅 핀은 GND. 데이터·제어선은 어댑터에서 **33Ω 직렬 저항**(`R_Pack04` ×6)을 경유한다.

**배선 시 주의**

- **1번 핀 방향.** 40P-A는 접점이 아래를, 40P-B는 위를 향한다. 전기적 배열은 같지만 삽입 방향이
  반대라 핀 번호가 뒤집혀 보인다. 배선 전 패널 FPC의 1번 표식을 반드시 육안 확인하고,
  전원 인가 전 도통 시험을 한다.
- **GND가 3개뿐이다** (9, 12, 22). 16비트 버스를 40.5 MHz로 돌리기에는 리턴 경로가 빈약하다.
- **BORDER(40)는 FPGA가 구동하지 않는다.** 패널 테두리 전극이며 보통 VCOM에 묶거나 PMIC에서
  별도 공급한다. 띄워두면 가장자리에 얼룩이 남는다.
- **VCOM(10)은 패널별 고유값이다.** FPC 인쇄값을 읽거나 킥백 측정으로 잡는다.

### 1.3 시스템 구성

`README.md`가 기술한 구조 그대로다. **XIAO가 전원(고전압 레일)을 만들고, FPGA가 EPD 신호를 낸다.**
둘은 핸드셰이크 2선으로만 연결된다.

```
[ HDMI 소스 ] --(DVI/HDMI)--> [ Tang Primer 20K ] --(EPD 병렬 23선)--+
                                    |         |                      |
                                    +--(DDR3)-+                      v
                                  (프레임버퍼)                [ ED115OC1 패널 ]
                                       ^                             ^
                    PMIC_READY (N7)  <--|                            |
                    REFRESH_DONE (N9) --|                            |
                                       |                     (전원 7선)
                                [ XIAO + EE03 PMIC ] --------------+
```

역할 분담:

| 담당 | 신호 |
|-|-|
| **FPGA** (Tang Primer 20K) | `ED0`~`ED15`, `SDCLK`, `SDLE`, `SDOE`, `SDCE0`, `GDCLK`, `GDSP`, `GDOE` — 총 23선 |
| **XIAO + EE03** | `+3V3`, `VPOS`, `VNEG`, `VGH`, `VGL`, `VCOM`, `BORDER` — 총 7선 |
| **핸드셰이크** | `PMIC_READY` (XIAO D1 → FPGA N7), `REFRESH_DONE` (FPGA N9 → XIAO D2) |

전원과 신호가 패널 FPC에서 합류하므로, **두 소스의 GND가 반드시 공통이어야 한다.**

### 1.4 EE03 보드 실물 구조

`XIAO_ePaper_Display_Board_EE03_V1.0_SCH_251217.pdf`(8페이지)에서 확인한 내용이다.

| 부품 | 역할 |
|-|-|
| **TPS651851RSLR** | EPD PMIC. VPOS/VNEG/VGH/VGL/VCOM 생성. I2C 0x68 |
| **IT8951E/DX** | EPD TCON. 우회 대상 |
| TPS22916CYFPR | 로드 스위치 (XIAO GPIO43) |
| TPS631000DRLR | 벅부스트 |
| BQ24070RHLR | 배터리 충전 |
| **AFC24-S40FIA-00** | **40핀 FPC 커넥터** |

> **커넥터가 Glider 40P 어댑터와 동일하다.**
> `pcb/40p-adapter-ab/adapter.kicad_sch`의 J1도 `AFC24-S40FIA-00`이다.
> [1.2](#12-40핀-커넥터-핀맵)의 핀맵이 EE03에도 그대로 적용될 가능성이 높다.

#### 문제 1: IT8951이 같은 버스를 구동한다

EE03의 IT8951은 `SDDO0`~`SDDO15`, `SDCLK`, `SDLE`, `SDOE`, `SDCE_N0`~`N3`,
`GDCLK`, `GDOE`, `GDSPV`, `GDRL`, `MODE`, `BORDER`를 전부 직접 구동한다.
FPGA가 같은 선을 구동하면 **버스 경합**이 나고 양쪽 드라이버가 손상될 수 있다.

"IT8951을 우회한다"를 리셋만으로 해결할 수 없다. 리셋 상태의 칩도 핀을 완전히 놓는다는 보장이 없고,
현재 XIAO 코드는 오히려 리셋을 해제한다(`xiao_pmic_controller.ino:53`).

다만 회로도에 `SDDO0` → `SDDO0_OUT` 같은 **`_OUT` 접미사 넷 쌍**이 있다.
IT8951과 커넥터 사이에 직렬 소자(0Ω 또는 33Ω)가 들어가 있다는 뜻이고, 이게 분리 지점이 된다.

#### 문제 2: PMIC 제어핀이 IT8951 뒤에 있다

이게 더 까다롭다. 회로도의 넷 이름이 다음 연결을 시사한다.

| IT8951 핀 | 넷 | TPS651851 핀 |
|-|-|-|
| GPIO8 / PWR0 | `PWR0_INT` | INT |
| GPIO10 / PWR2 | `PWR2_WAKEUP` | **WAKEUP** |
| GPIO11 / PWR3 | `PWR3_PWRCOM` | PWRCOM |
| GPIO12 / PWR4 | `PWR4_PWRUP` | **PWRUP** |
| GPIO13 / PWR5 | `PWR5_GOOD` | PWR_GOOD |

**TPS65185는 `WAKEUP`이 High여야 I2C에 응답하고, `PWRUP`으로 레일 시퀀스를 시작한다.**
그런데 이 두 핀이 XIAO가 아니라 IT8951의 GPIO에 물려 있다.
즉 IT8951을 죽이면 PMIC 제어권도 같이 잃는다.

현재 XIAO 코드가 I2C만으로 PMIC를 켜려 하는데 검증 코드가 하나도 없어서
(정의만 해두고 한 번도 호출하지 않는 `pmic_read_reg`) 실제로 동작하는지 알 수 없는 상태다.

> 넷 이름 기반 추론이다. PDF 텍스트 레이어에서 추출한 것이라 신호 존재와 명명 규칙은 확실하지만,
> **핀 대 핀 연결은 회로도를 눈으로 열어 확인할 것.** 특히 `WAKEUP`/`PWRUP`에 풀업이 있는지,
> XIAO 쪽으로 직결된 경로나 테스트포인트가 있는지가 중요하다.

#### 채택한 배선: 전원만 인출 (A안)

**패널 FPC를 EE03에 꽂지 않는다.** 별도 40핀 브레이크아웃에서
FPGA 신호 23선과 EE03에서 끌어온 전원 7선을 합류시킨다.

```
[ EE03 ] --(전원 7선)--+
                       |
                       v
             [ 40핀 브레이크아웃 ] --(40핀 FPC)--> [ ED115OC1 ]
                       ^
                       |
[ Tang Primer 20K ] --(신호 23선)--+
```

**문제 1(버스 경합)은 이걸로 해결된다.** EE03의 IT8951 출력은 아무것도 꽂히지 않은
자기 커넥터로 나가므로 패널에 닿지 않는다.

**문제 2(PMIC 제어핀)는 남는다.** 오히려 더 명확해진다 —
IT8951은 여전히 EE03 위에서 살아 있고 `WAKEUP`/`PWRUP`을 쥐고 있다. 두 선택지가 있다.

> **갱신 2026-08-25 — 권장안이 뒤집혔다. `IT8951을 살려두는 쪽`이 맞다.**
>
> 회로도를 열어 확인한 결과 **PMIC의 I2C가 XIAO에 닿지 않는다.** TPS651851의
> SCL/SDA(시트 6, 핀 17·18)는 `ITE_I2C_*`이고 이것은 IT8951이 마스터인 버스다.
> XIAO의 I2C(GPIO41/42)에는 SHT40(0x44)만 달려 있다. 즉 XIAO에서 `0x68`로 보낸
> I2C는 전부 허공으로 갔다.
>
> 그런데 **XIAO는 IT8951과 SPI로 이미 연결돼 있고**, Seeed_GFX가 그 경로로
> VCOM을 설정하는 명령을 이미 구현해 두었다(`setTconVcom`, IT8951 명령 `0x39`).
> 제어권을 뺏는 대신 IT8951을 시키면 **납땜이 전혀 필요 없다.**
>
> A안과 충돌하지 않는다. 패널 FPC를 EE03에 꽂지 않으므로 IT8951의 출력은 아무
> 데도 가지 않고, 이 보드에서 나가는 것은 전원 7선뿐이다. IT8951은 EE03 위에서
> PMIC 관리자로만 남는다.
>
> 구현: `xiao_pmic_controller/ee03_rails/`. Seeed_GFX의 `ED115OC1_Test` 예제를
> 기반으로 하되 프레임 스프라이트를 할당하지 않고(2760×2070은 PSRAM 수 MB),
> `hostTconInit()`이 하드코딩한 `setTconVcom(1400)`을 **1310**으로 덮어쓴다.
> 아래 표는 이 발견 이전의 판단이며 기록으로 남긴다.

| | **IT8951을 살려둠 (현재 권장)** | ~~IT8951을 리셋에 붙잡음~~ |
|-|-|-|
| `WAKEUP`/`PWRUP` | IT8951이 자기 초기화 과정에서 구동 | **XIAO에서 직접 배선 필요** |
| PMIC 제어권 | IT8951과 XIAO가 **이중 마스터**로 충돌 | XIAO 단독 |
| 위험 | IT8951이 임의로 VCOM을 바꾸거나 자체 타임아웃으로 레일을 내릴 수 있음 | 없음 |

IT8951을 살려두면 PMIC에 마스터가 둘이 된다. 브링업 중 원인을 알 수 없는 전원 차단이나
VCOM 변동이 생기면 거의 이것 때문이다. **리셋에 붙잡고 XIAO가 단독 제어하는 쪽을 권장한다.**

리셋 상태에서는 IT8951의 GPIO가 Hi-Z가 되므로 `WAKEUP`/`PWRUP`이 뜬다.
따라서 이 두 선을 XIAO GPIO로 반드시 끌어와야 한다.

> **현재 전원이 올라오는 것은 IT8951 덕분이다.**
> 지금 펌웨어는 `TFT_RST`를 HIGH로 놓아 IT8951을 기동시키고,
> IT8951이 `WAKEUP`/`PWRUP`을 구동해서 레일이 올라온다([9장](#9-파일별-수정-목록)).
> **2선 배선 전에 리셋을 걸면 전원이 아예 안 올라온다.**
> 순서: ① `WAKEUP`/`PWRUP` 배선 → ② 펌웨어에서 두 핀 구동 확인 → ③ `TFT_RST` LOW 고정.

**필요한 추가 배선 2선**

| 넷 | TPS651851 핀 | 연결 | 비고 |
|-|-|-|-|
| `PWR2_WAKEUP` | WAKEUP | XIAO GPIO | High여야 I2C 응답. 파워업 맨 앞에서 assert |
| `PWR4_PWRUP` | PWRUP | XIAO GPIO | 레일 시퀀스 트리거 |

`PWR5_GOOD`(PWR_GOOD)는 I2C 레지스터 `0x0F`로 읽을 수 있으므로 별도 배선이 필수는 아니다.

탭 지점은 IT8951의 해당 GPIO 핀, 또는 그 넷의 저항 패드/테스트포인트다.
**회로도를 눈으로 열어 확인할 것.**

#### 전원 인출 방법

EE03의 40핀 커넥터 자체가 전원 탭 지점으로 가장 깔끔하다 —
[1.2](#12-40핀-커넥터-핀맵)의 핀 1, 3, 5, 10, 11, 36, 38, 40이 정확히 필요한 레일이다.

브레이크아웃에 40핀 커넥터를 두 개(EE03용 / 패널용) 두고,
EE03 쪽 커넥터에서는 **전원 핀만 패널 쪽으로 잇고 신호 핀은 연결하지 않는다.**
이러면 EE03에 납땜을 하지 않아도 된다.

**전기적 주의**

- **GND를 확실히 공통으로 묶는다.** EE03와 Tang Primer의 GND를 브레이크아웃에서 여러 가닥으로
  연결한다. 전원과 신호가 서로 다른 소스에서 오므로 리턴 경로가 부실하면 40.5 MHz 신호가 깨진다.
- **고전압선과 신호선을 같은 리본에 나란히 놓지 않는다.** VPOS/VNEG는 프레임마다 스위칭하고
  VCOM은 노이즈에 민감하다. 전원 다발과 신호 다발을 물리적으로 분리한다.
- **EE03의 전류 용량을 확인한다.** 루트 README는 대형 패널을 위해 ±15V 레일에 최대 1A 피크를
  권장한다. EE03는 XIAO용 소형 보드라 TPS651851 주변 인덕터·부품이 그만한 전류를 상정하지
  않았을 수 있다. ED115OC1은 2760×2070으로 큰 편이다.
  전면 흑↔백 전환(피크 전류 조건)에서 VPOS/VNEG 드룹을 오실로스코프로 측정해 검증한다.
  부족하면 외부 EPD 전원으로 교체해야 한다.

### 1.5 전원 레일

| 레일 | 전압 | 40핀 |
|-|-|-|
| +3V3 | 3.3V | 5, 11 |
| VPOS | +15V | 36 |
| VNEG | −15V | 38 |
| VGH | +22V | 3 |
| VGL | −20V | 1 |
| VCOM | −3V ~ 0V (가변) | 10 |
| BORDER | 통상 VCOM | 40 |

**전원 시퀀싱이 안전 요건이다.** FPGA 쪽이 `PWR_GOOD` 전까지 `SDOE`/`GDOE`를 Low로 잡고 있어야 한다.
현재 코드는 `caster.v:821, 832`에서 둘 다 상수 1이고 `top.v:424`의 `sys_ready`도 하드코딩 1이라
레일이 올라오는 중에 패널이 구동된다. 반드시 고친다.

### 1.6 FPGA 보드와 배선

- Sipeed Tang Primer 20K Core Board + Dock (GW2A-LV18PG256C8/I7, 온모듈 128MB DDR3)
- 패널 로직이 3.3V, PMOD도 3.3V LVCMOS라 **레벨 시프터는 불필요**하다.
- 현재 `constraints/gowin_constraints.cst`가 이미 `EPD_SD[15:0]` 16핀 + 제어 7핀을
  PMOD0~PMOD2에 할당해 두었다. **핀 배정은 변경 불필요.**
- 실질적 위험은 신호 무결성이다. SDCLK 40.5 MHz + 16비트 데이터를 PMOD 점퍼 다발로 보내면
  스큐가 깨진다. 33Ω 직렬 저항을 넣고, 짧은 리본 + GND 리턴을 쓰고,
  가능하면 PMOD→40핀 FPC 변환 PCB를 만든다.

---

## 2. 왜 2배 확대인가

Caster는 픽셀당 **4.5바이트**의 VRAM 트래픽을 쓴다 — 상태 읽기 2B + 상태 쓰기 2B + 입력 이미지 읽기 0.5B.
상태는 매 프레임 갱신되므로 이 트래픽은 프레임레이트에 정비례한다.

### 2.1 메모리 대역폭

Tang Primer 20K의 DDR3는 16비트 × 800 Mbps = **1600 MB/s 이론 피크**,
실효 70%로 잡으면 약 **1120 MB/s**가 천장이다.

| 구성 | 대역폭 | 판정 |
|-|-|-|
| 풀해상도 2760×2070 @ 60 Hz | 1543 MB/s | 초과 |
| 풀해상도 2760×2070 @ 50 Hz | 1285 MB/s | 초과 |
| 2배 확대 1280×960 @ 60 Hz | **332 MB/s** | 30% 사용 |
| 2배 확대 1280×960 @ 70 Hz | 387 MB/s | 35% 사용 |

풀해상도는 천장 아래로 내려가려면 40 Hz 이하여야 한다. 그런데 루트 README가 명시한다 —

> 계조 표현에는 화면을 85Hz로 리프레시해야 한다. 85Hz가 E Ink가 지원하는 리프레시 레이트이고,
> 60Hz는 경우에 따라 노력하면 동작시킬 수 있다.

즉 **풀해상도는 쓸 만한 어떤 프레임레이트에서도 성립하지 않는다.**

README가 제시하는 메모리 구성별 한계표(90% 대역폭 활용 가정)로도 같은 결론이 나온다.
`DDR2/3-800 x16 = 360 MP/s`가 Tang Primer 20K와 정확히 같은 구성이다.

| 구성 | 필요 처리량 | README 한계 | 판정 |
|-|-|-|-|
| 풀해상도 @ 60 Hz | 363 MP/s | 360 MP/s | 초과 |
| 2배 확대 @ 60 Hz | 74 MP/s | 360 MP/s | 20% 사용 |

### 2.2 처리 속도

원본 Caster는 Spartan-6에서 디더링 켜고 133 MP/s, 꺼도 280 MP/s가 상한이다.
ED115OC1 풀해상도 60 Hz는 **343 MP/s**를 요구한다 — 원본 설계의 상한 자체를 넘는다.
README도 이 영역은 "8-wide 설계로 확장해야 500 MP/s"라고 적고 있다.
GW2A-18(55nm)은 Spartan-6(45nm)보다 느리므로 여유는 더 없다.

2배 확대를 하면 실제 처리량이 **74 MP/s**로 떨어진다. 코어 클럭 40.5 MHz에서 4px/clk,
그것도 짝수 라인에서만 동작하므로 타이밍 클로징이 편하다.

### 2.3 치르는 대가

- 유효 화소 밀도가 네이티브 300 PPI에서 **150 PPI**로 떨어진다.
- 패널 면적의 **86%**만 쓴다 (2560×1920 / 2760×2070). 우측 200px, 하단 150줄이 미스캔이다.

전자잉크는 쌍안정이라 스캔하지 않는 영역은 **이전 상태를 그대로 유지**한다. 검게 남는 게 아니라
직전에 무엇이 표시돼 있었든 그게 남는다. 따라서 **최초 1회 전면 클리어가 필요하다** —
가장자리를 흰색으로 한 번 몰아두면 이후로는 흰 여백처럼 보인다.
Caster의 `OP_EXT_REDRAW`로 처리하거나 브링업 4단계에서 전면 백색 패턴을 한 번 흘린다.

이 손실은 Tang Primer 20K를 쓰는 한 우회할 수 없다. 네이티브가 필요하면
DDR3 채널이 2개 이상인 보드로 가야 한다.

---

## 3. 목표 스펙

### 3.1 운용 해상도 선정

16비트 버스에서 SDCLK 1사이클 = 16bit ÷ 2bpp = **패널 픽셀 8개**다.
2배 가로 확대를 걸면 그 8픽셀은 **고유 픽셀 4개**에서 나온다 — 이게 정확히 Caster 코어의 1워드다.
따라서 **SDCLK 1사이클 = 코어 1워드**로 맞아떨어진다.

`1280×960`이 모든 제약을 동시에 만족한다.

| 조건 | 1280×960 | 판정 |
|-|-|-|
| W × H 가 128의 배수 (memif 256B 커맨드 단위) | 1,228,800 ÷ 128 = 9600 | 충족 |
| 2배 확대 후 패널 안에 들어감 | 2560×1920 ≤ 2760×2070 | 충족 |
| 호스트가 낼 수 있는 표준 모드 | VESA 1280×960 | 커스텀 EDID 불필요 |
| DVI 직접 디시리얼라이저 105 MP/s | 85 MP/s @ 60 Hz | 여유 19% |
| 현재 코드 값과의 차이 | `INPUT_*`, `FBYTES` 동일 | 변경 불필요 |

`memif.v`는 커맨드당 16버스트 × 16바이트 = **256바이트** 단위로만 VRAM을 오간다.
따라서 `FBYTES = W × H × 2`가 256의 배수여야 하고, 곧 **W × H가 128의 배수**여야 한다.
루트 README도 같은 제약을 명시한다.

### 3.2 타이밍 파라미터

| 항목 | 값 | 도출 |
|-|-|-|
| 패널 물리 해상도 | 2760 × 2070 | 네이티브 |
| 패널 스캔 영역 | 2560 × 1920 | 면적 86% |
| 입력 / VRAM 격자 | 1280 × 960 | VESA 표준 |
| 라인당 스캔 스텝 | 320 | 1280 ÷ 4 = 2560 ÷ 8 |
| htotal | 344 | 320 + HFP 20 + HSYNC 2 + HBP 2 |
| vtotal | 1927 | 1920 + VFP 4 + VSYNC 1 + VBP 2 |
| 프레임당 스캔 클럭 | 662,888 | 344 × 1927 |
| 60 Hz 필요 최소 SDCLK | 39.8 MHz | 662,888 × 60 |
| 코어 클럭 = SDCLK | **40.5 MHz** | rPLL 27 × 3/2 — 필요 최소 대비 1.8% 여유 |
| vsync 주기 | 675,000 clk | 40.5 MHz ÷ 60.00 Hz (`top.v:162`) |
| 프레임버퍼 | 2,457,600 B | 320 × 4 × 960 × 2 |
| DDR3 사용률 | 332 MB/s | 천장 대비 30% |

`HFP`가 20인 것은 블랭킹을 줄이지 않은 게 아니라 **GDCLK low 펄스 494 ns를 확보하기 위한
의도된 값**이다([7.6](#76-튜닝-노브)). 데이터시트 없이 정한 추정치라 실측 후 조정 대상이고,
줄이면 그만큼 필요 SDCLK가 내려간다 — HFP 4까지 줄이면 htotal 328, 60 Hz에 37.9 MHz면 된다.
다만 **세로로 밀리는 증상이 나오면 오히려 키워야 하므로, 현재는 여유 있는 쪽으로 잡아 둔다.**
`HSYNC` 2 / `HBP` 2는 루트 README의 TCON 가이드 기본값이다. 화면이 밀려 보이면 HBP·VBP를 조정한다.

스캔 662,888 clk와 vsync 주기 675,000 clk 사이의 12,112 clk 차이는 버그가 아니라
설계상 정상이다 — [3.4](#34-스캔-타이밍과-입력-타이밍의-관계) 참조.

### 3.3 프레임레이트 여유

필요 SDCLK는 `662,888 × FR`이다.

| FR | 필요 SDCLK / 코어 | DDR3 | DVI 입력 | 판정 |
|-:|-:|-:|-:|-|
| 50 Hz | 33.1 MHz | 277 MB/s | 71 MP/s | 패널 SDCLK가 낮을 때 후퇴선 |
| **60 Hz** | **39.8 MHz** | **332 MB/s** | **85 MP/s** | **목표 — 현재 rPLL 40.5 MHz로 충족** |
| 70 Hz | 46.4 MHz | 387 MB/s | 99 MP/s | 여유 있음 — 패널이 받아주면 상향 |
| 85 Hz | 56.3 MHz | 470 MB/s | 121 MP/s | DVI 입력 초과 |

현재 rPLL 40.5 MHz는 스캔만 놓고 보면 61.1 Hz까지 낼 수 있다.
70 Hz로 올리려면 rPLL 재설정(예: 27 × 7/4 = 47.25 MHz)이 필요하다.

Tang Primer 20K는 ADV7611 없이 TMDS를 직접 받으므로 README의
"DVI 직접 디시리얼라이저 105 MP/s" 한계가 적용된다. Caster는 스캔을 입력 VSYNC에 물리므로
입력 프레임레이트 = 패널 프레임레이트이고, 여기가 상한이 된다.

DDR3는 어느 쪽이든 병목이 아니다. 병목은 **패널 SDCLK**와 **DVI 입력 대역** 둘이고,
E Ink 규정 85 Hz는 DVI에서 막히지만 70 Hz까지는 열려 있다.
목표는 60 Hz로 잡되, 패널 SDCLK가 46.4 MHz를 받아주면 70 Hz로 올린다 — 계조 품질이 낫다.

### 3.4 스캔 타이밍과 입력 타이밍의 관계

프레임당 스캔은 662,888 clk인데 vsync 주기는 675,000 clk다. 12,112 clk(1.8%, 0.30 ms)가
남는다. **이 여유는 정상이며, Caster 아키텍처가 요구하는 것이다.** 루트 README가 명시한다.

> The EPDC runs a local Eink timing generator for counting pixels and blankings.
> **This timing should be a slightly delayed version of the incoming video timing.**
> (`README.md:845`)

EPDC의 로컬 타이밍은 입력 비디오 타이밍과 같은 것이 아니라 **살짝 뒤처진 복사본**이다.
스캔이 먼저 끝나고 남는 시간은 다음 vsync를 기다리는 데 쓴다.

README의 TCON 파라미터 도출 공식(`README.md:1122-1131`)이 이 여유를 직접 만들어낸다.

```
tcon_hfp = hblk/4 - tcon_hsync - tcon_hbp      # 가로: 나머지를 전부 HFP로 흡수
tcon_vfp = vblk - vfp - tcon_vsync - tcon_vbp  # 세로: vfp만큼 빼고 흡수
```

README 예제(2200×1648 @42 Hz)에 넣으면 의도가 드러난다.

| | 비디오 | TCON | 차 |
|-|-|-|-|
| htotal | (2200+80) ÷ 4 = **570** | 550+16+2+2 = **570** | **0** |
| vtotal | 1648+33 = **1681** | 1648+11+1+2 = **1662** | **19 = vfp** |

**가로는 정확히 일치시키고, 세로만 짧게 만든다.** 가로가 어긋나면 라인마다 오차가 누적돼
입력 FIFO가 터지므로 딱 맞춰야 하고, 세로 잉여는 프레임 끝의 vsync 대기로 흡수된다.

#### 1단계에서는 무해하다

지금 675,000은 입력이 없어서 "40.5 MHz에서 60.00 Hz"로 역산한 값이고, 662,888과의 차이는
유도된 것이 아니라 떨어진 나머지다. 입력이 없으므로 스캔이 먼저 끝나고 노는 것뿐이다.
2단계에서 vsync가 `dvi_rx`에서 오면 `top.v:138-154`의 내부 생성기와 함께 이 값 자체가 사라진다.

#### 2단계에서는 재유도해야 한다

세로 2배 확대 때문에 README 공식을 그대로 쓸 수 없다. 입력 1라인이 패널 2라인이 되므로,

> **EPDC 라인레이트 = 입력 라인레이트 × 2** — 단, 정확히 2배가 아니라 **약간 느린 쪽**이어야 한다.

현재 값으로 검산하면 안전한 쪽에 있다.

```
입력 (1280x960 CVT-RB, 85.2 MHz, htotal 1440) = 59.17 kHz/line
        x 2                                   = 118.3 kHz
EPDC (40.5 MHz, htotal 344)                   = 117.7 kHz/line   -> 0.5% 느림
```

**방향이 중요하다.** EPDC가 입력보다 빠르면 아직 도착하지 않은 픽셀을 꺼내려 하므로
입력 FIFO가 언더런하고 프레임이 깨진다. 느린 쪽은 매 라인 조금씩 뒤처지다가 vsync에서
리셋되므로 안전하다 — README가 "slightly **delayed**"라고 못 박은 이유가 이것이다.

라인당 필요한 FIFO 깊이도 여기서 나온다. 활성 라인 중 EPDC는 320워드를 320클럭
(7.9 µs)에 소비하는데 입력은 같은 320워드를 15.0 µs에 걸쳐 보낸다.
따라서 `vi_fifo`는 **최소 1라인(320워드)을 버퍼링해야 한다.**

---

## 4. 클럭 설계

현재 `rtl/sysclock.v`는 PLL 없는 단순 통과라 27 MHz 하나로 전부 돌고 있다.
`locked`는 `initial` + `#100`으로 만들어져 합성 시 상수 1로 최적화된다
(yosys가 모듈을 통째로 삭제한다). 결과적으로 **리셋이 존재하지 않는다.**

| 도메인 | 주파수 | 소스 | 담당 |
|-|-|-|-|
| `clk_epdc` | 40.5 MHz | rPLL (27 MHz 입력) | 스캔 카운터, 처리 파이프라인, 출력단, SDCLK |
| `clk_mem` | 100 MHz | DDR3 IP `clk_out` (memory_clk ÷ 4) | `memif`, DDR3 유저 인터페이스 |
| `clk_vi` | ~85 MHz | HDMI TMDS 복원 | `dvi_rx`, 입력 픽셀 |
| `memory_clk` | 400 MHz | rPLL (DDR3 IP 입력) | DDR3 PHY (DDR3-800) |

도메인이 넷으로 갈리므로 `bi_fifo.v` / `bo_fifo.v`를 반드시 교체해야 한다
([6.4](#64-비동기-fifo-교체)).

리셋은 `rPLL.lock && init_calib_complete`를 받아 각 도메인으로 동기 해제하는 시퀀서를 새로 만든다.
`top.v:465`의 `wire epdc_rst = 1'b0;` 하드코딩도 제거한다.

---

## 5. 출력단 재설계

### 5.1 현재 구조의 문제

현재 포트는 가로 2배 확대를 **파이프라인을 2사이클 멈추는 방식**으로 구현했다
(`caster.v:408`의 `h_cnt_offset[0] == 1'b0` 게이팅 + `caster.v:741`의 `current_pixel` 홀드).

그런데 8비트 모드에서 1워드는 픽셀 4개다. 같은 워드를 두 번 내보내면

```
a0 a1 a2 a3  a0 a1 a2 a3      <- 현재 (4픽셀 블록 반복)
a0 a0 a1 a1  a2 a2 a3 a3      <- 최근접 확대 (올바른 결과)
```

가 된다. **4픽셀 블록이 통째로 반복되는 버그**다.

### 5.2 2배 확대를 배선으로

16비트 버스로 가면 배선 한 줄로 정확하게 풀린다. 코어 워드의 각 2비트 필드를 두 번씩 복제한다.

```verilog
// 코어 1워드 = 4픽셀 x 2bit = 8bit
// 각 2bit 필드를 복제 -> 16bit = 패널 픽셀 8개
assign epd_sd = {
    epd_out_pixel[7:6], epd_out_pixel[7:6],   // p3 p3
    epd_out_pixel[5:4], epd_out_pixel[5:4],   // p2 p2
    epd_out_pixel[3:2], epd_out_pixel[3:2],   // p1 p1
    epd_out_pixel[1:0], epd_out_pixel[1:0]    // p0 p0
};
```

이 변경 하나가 세 가지를 동시에 해결한다.

- **블록 반복 버그가 사라진다** — 진짜 최근접 2배 확대가 된다.
- **파이프라인 스톨이 불필요해진다** — 가로 방향 `h_cnt_offset[0]` 게이팅을 제거할 수 있다.
- **SDCLK = 코어 클럭**이 된다 — 기존 `OUTPUT_16B` 경로는 `out_clk = clk/2`라 코어를 2배로
  돌려야 했다.

세로 2배 확대는 현재의 라인 버퍼 방식(`caster.v:754-781`)이 맞게 동작하므로 그대로 둔다.
짝수 출력 라인에서 `scale_line_buf`에 쓰고 홀수 라인에서 되읽어 재생한다.
결과적으로 처리 파이프라인은 **짝수 라인에서만 동작(50% duty)** 한다.

가로 게이팅이 사라지면 스캔 카운터의 1스텝이 다시 "고유 픽셀 4개"를 의미하게 되므로,
현재 포트가 `>> 1` / `[3:1]`로 바꿔둔 것들 중 **가로 관련은 업스트림으로 되돌리고
세로 관련만 절반을 유지**한다. 이 구분이 틀리면 디더링 패턴과 영역 업데이트 좌표가 어긋난다.

### 5.3 SDCLK 출력

`caster.v:813`의 `assign epd_sdclk = ~clk;`는 일반 로직 경로로 클럭을 IO에 내보내는 것이라
데이터 대비 스큐가 크다. 27 MHz면 버티지만 40.5 MHz에서는 위험하다.
원본 Spartan-6 코드가 `ODDR2`를 쓰던 자리이므로, Gowin `ODDR` 프리미티브로 교체한다.

---

## 6. DDR3 서브시스템

### 6.1 현재 상태

`rtl/mig_wrapper.v`는 DDR3 컨트롤러 인스턴스가 없는 스텁이다.

```verilog
assign ddr_calib_done = 1'b1;   // 캘리브레이션 가짜 완료
assign mig_rd_empty   = 1'b0;   // 항상 데이터 있는 척
reg [127:0] data_reg;           // 128비트 레지스터 1개 루프백
```

yosys로 합성하면 `DDR_A`, `DDR_BA`, `DDR_RAS_N`, `DDR_CAS_N`, `DDR_WE_N`, `DDR_CKE`,
`DDR_ODT`, `DDR_CK_P/N`, `DDR_LDM/UDM`이 전부 `used but has no driver` 경고를 내고,
`DDR_DQ`/`DQS` inout은 IOBUF조차 생성되지 않는다.

결과적으로 `memif`가 읽어오는 "이전 픽셀 상태"는 항상 직전에 쓴 128비트 워드 하나다.
Caster 저지연 구조의 핵심인 픽셀별 상태·타이머 추적이 존재하지 않으므로,
웨이브폼 룩업, early cancellation, 하이브리드 계조가 전부 무의미해진다.

### 6.2 Gowin DDR3 IP

`TangPrimer-20K-example-main/DDR-test/`의 `DDR3_Memory_Interface_Top`을 쓴다.
IP 설정(`.ipc`)은 다음과 같다.

```
target_device = gw2a18c-011
MEMORY_CLK    = 400          # DDR3-800
CLK_RATIO     = 1:4          # 유저 클럭 = 100 MHz
BURST_MODE    = 1
BURST_TYPE    = 0
```

유저 인터페이스:

| 신호 | 폭 | 설명 |
|-|-|-|
| `clk_out` | 1 | 유저 클럭 100 MHz (= `clk_mem`) |
| `init_calib_complete` | 1 | 캘리브레이션 완료 |
| `cmd_en` / `cmd` / `cmd_ready` | 1 / 3 / 1 | 커맨드 발행 핸드셰이크 |
| `addr` | 27 | 주소 (Gowin은 28로 선언, 유효 27) |
| `app_burst_number` | 6 | 버스트 길이 − 1 |
| `wr_data_en` / `wr_data` / `wr_data_end` / `wr_data_rdy` | 1 / **128** / 1 / 1 | 쓰기 |
| `wr_data_mask` | **16** | 바이트 마스크 |
| `rd_data` / `rd_data_valid` / `rd_data_end` | **128** / 1 / 1 | 읽기 |

### 6.3 memif 어댑터

**`memif.v`가 업스트림과 완전히 동일하므로 스트리밍 시퀀서 로직은 손댈 필요가 없다.**
IP 포트에 맞추는 shim만 쓰면 되고, 다행히 규약이 상당 부분 그대로 맞는다.

| 항목 | `memif.v` | Gowin IP | 일치 |
|-|-|-|-|
| 데이터 폭 | 128비트 | 128비트 | 그대로 |
| 마스크 폭 | 16비트 | 16비트 | 그대로 |
| 쓰기 커맨드 | `mig_cmd_instr = 3'b000` | `WR_CMD = 3'h0` | 그대로 |
| 읽기 커맨드 | `mig_cmd_instr = 3'b001` | `RD_CMD = 3'h1` | 그대로 |
| 버스트 규약 | `mig_cmd_bl = BURST_LENGTH - 1` | `app_burst_number = N - 1` | 그대로 |
| 버스트 길이 | 16 워드 = 256 B | `app_burst_number = 15` | 그대로 |
| 캘리브레이션 | `ddr_calib_done` | `init_calib_complete` | 이름만 다름 |

**실제로 shim이 해야 할 일은 두 가지뿐이다.**

1. **흐름 제어 방식 변환.** `memif`는 FIFO 스타일(`cmd_full`, `wr_full`, `rd_empty`)을 기대하고
   Gowin IP는 핸드셰이크 스타일(`cmd_ready`, `wr_data_rdy`, `rd_data_valid`)을 쓴다.
   각각에 작은 FIFO를 두고 변환한다.

   ```
   mig_cmd_full   <= !cmd_ready
   mig_wr_full    <= !wr_data_rdy
   mig_rd_empty   <= !rd_data_valid   (읽기 FIFO 경유)
   ```

2. **주소 단위 확인.** `memif`는 바이트 주소를 낸다(`mig_cmd_byte_addr`, 30비트 중 24비트 사용).
   Gowin `addr`는 27비트다. 128MB = 2^27이므로 바이트 주소 직결일 가능성이 높으나
   하위 4비트(16바이트 정렬) 취급이 IP 설정에 따라 다르다.
   **브링업 5단계에서 write → read-back 테스트로 반드시 검증한다.**

`memif.v`는 커맨드당 256바이트씩 요청하므로 버스트가 충분히 길어 DDR3 효율이 잘 나온다.
필요 대역폭 332 MB/s는 IP 피크 1.6 GB/s의 **21%**라 여유가 크다.

### 6.4 비동기 FIFO 교체

현재 `bi_fifo.v` / `bo_fifo.v`는 두 가지 문제가 있다.

- **포인터가 그레이코드가 아니다.** 지금은 `clk_mif == clk_epdc == 27 MHz`라 가려져 있지만,
  DDR3 IP를 붙이는 순간 `clk_mem` 100 MHz와 `clk_epdc` 40.5 MHz로 갈라지면서 깨진다.
- **인덱스 범위 초과.** `bi_fifo.v:21-22`가 `mem[0:255]`를 `{wr_ptr[7:0], 1'b0}` 9비트 인덱스로
  접근한다. 시뮬레이션(범위 밖 무시)과 합성(하위 비트 절단)의 동작이 다르다.

비대칭 폭(쓰기 128비트 / 읽기 64비트, 그 반대)을 유지하는 **그레이코드 비동기 FIFO**로 교체한다.

---

## 7. DDR3 없이 구동하기 — 1단계 목표

HDMI 입력이 아직 없으므로, 1단계 목표는 **FPGA가 스스로 만든 데모 영상으로 패널을 고주사율
구동하는 것**이다. 이 단계에서는 DDR3가 필요 없다.

### 7.1 왜 DDR3 없이 되는가

DDR3에 담기는 것은 이미지가 아니라 **픽셀별 16비트 상태**다([6장](#6-ddr3-서브시스템)).
그 상태가 필요한 이유는 단 하나 — PC에서 임의의 영상이 들어올 때
*각 픽셀이 지금 어디까지 구동됐는지* 알아야 하기 때문이다.

데모 영상은 FPGA가 직접 생성하므로 **모든 픽셀의 목표값을 이미 알고 있다.**
따라서 상태를 저장할 필요 없이 매 사이클 화면 전체를 새로 그리면 된다.
Caster의 저지연 기법(early cancellation, 하이브리드 계조)은 쓸 수 없지만,
패널 배선 · 전원 · 타이밍 · 프레임레이트는 전부 그대로 검증된다.

### 7.2 구동 방식

`caster.v`의 `PANEL_TEST` 블록이 파이프라인과 VRAM을 통째로 우회하고 출력을 직접 만든다.

```
DRIVE  12 프레임 (200 ms)   모든 픽셀을 목표값으로 구동
                            박스 = 흰색(VPOS 2'b10), 배경 = 검정(VNEG 2'b01)
REST   12 프레임 (200 ms)   무구동(2'b00), 패널 안정화
-> 박스가 8워드(32 px)씩 이동, 반복
```

Hi-Z(`2'b11`)는 어떤 경우에도 출력하지 않는다 — 시뮬레이션에서 0건을 확인했다.

### 7.3 빌드 타깃

| 타깃 | 용도 | PMIC | 파이프라인 |
|-|-|-|-|
| `make` | 정식 빌드 · FPGA 단독 점검 겸용 | BTN0로 게이트 | 전체 (DDR3 스텁) |
| **`make demo`** | **패널 데모 (1단계 목표)** | 필요 | 우회 |

> **갱신 2026-08-24**: `make bringup` 타깃은 없어졌다. `PMIC_READY` 신호 자체가
> 제거됐기 때문이다(XIAO 펌웨어가 부팅 직후 무조건 assert해서 아무 정보도
> 담지 않았다 — `top.v:55`). 그 역할은 **BTN0 안전 게이트**가 대신한다:
> 전원 인가 직후 EPD 버스는 조용하고, BTN0를 눌러야 GDOE/SDOE가 올라간다.
> **고전압 레일이 살아있는 상태에서 BTN0를 누르지 말 것.**

리소스 비교:

| 빌드 | LUT4 | BSRAM | Fmax |
|-|-|-|-|
| `make` | 51% | 27/46 (58%) | **78~86 MHz** (2026-08-24 실측) |
| `make demo` | **6%** | **0/46** | **105 MHz** |

> `make` 쪽 Fmax 62 MHz는 옛 값이다. 프론트 패널·UART 콘솔·버스 검사기가
> 추가된 뒤에도 78 MHz 이상 나온다. `RAM16SDP4`가 79%까지 올라와 있어,
> DDR3 IP와 `dvi_rx`가 들어오는 2단계에서 여기가 먼저 막힐 자리다.

데모 빌드는 파이프라인 · FIFO · 웨이브폼 LUT가 전부 최적화로 사라지고
스캔 타이밍 생성기만 남는다.

### 7.4 검증 3단계

**1단계 — 시뮬레이션 (하드웨어 없이)**

`iverilog`로 한 프레임을 세어 설정값과 대조한다.

| 항목 | 기대값 |
|-|-|
| 프레임 주기 (vsync 간격) | 675,000 clk = **60.0 Hz** |
| 그중 실제 스캔 구간 | **662,888 clk** — 나머지 12,112 clk는 vsync 대기 ([3.4](#34-스캔-타이밍과-입력-타이밍의-관계)) |
| GDCLK 상승 에지 | **1927** (= vtotal, 라인당 1개) |
| SDLE 상승 에지 | **1927** |
| SDCE0 low 사이클 | **614,400** (= 320 × 1920) |
| `2'b11` 출력 | **0건** |

**2단계 — FPGA 셀프테스트 (패널·PMIC 없이)**

`rtl/epd_selftest.v`가 EPD 출력 핀을 되읽어 매 프레임 위 세 카운트를 검사하고
**LED D2**로 보고한다. 계측기가 필요 없다.

| LED D2 | 의미 |
|-|-|
| 계속 켜짐 | EPD 버스 정상 |
| 1번 깜빡 | GDCLK 이상 |
| 2번 깜빡 | SDLE 이상 |
| 3번 깜빡 | SDCE0 이상 |

기대값은 `defines.vh`에서 자동 계산되므로 타이밍을 바꿔도 따라간다.

> **갱신 2026-08-24 — 주력은 UART 콘솔이다.** 깜빡임 코드는 어느 카운트가
> 틀렸는지만 말하고 *얼마나* 틀렸는지는 말하지 못한다. Dock의 FT2232 채널 B가
> FPGA M11에 이미 배선돼 있어 추가 배선 없이 텍스트로 전부 뽑을 수 있다
> (`/dev/cu.usbserial-1101`, 115200 8N1):
>
> ```
> G=787 S=787 A=096000 E=+000000 P=1 F=0 U=ff Z=0000 D=0000 V=0000 L=03c0
> ```
>
> `G/S/A/E/P/F`는 셀프테스트, `U/Z/D`는 `epd_sd_check.v`(16비트 버스·Hi-Z·가로
> 2배 배선), `V/L`은 `epd_line_dup.v`(세로 2배 라인 쌍)다. PROGRESS.md §1.3~1.6
> 참조. **포트를 연 채로 보율을 잡아야 한다** — 보드를 다시 꽂으면 termios가
> 9600으로 리셋되고 `stty`는 포트를 닫는 순간 날아간다.
>
> 이 방식으로 `EXP_ACTIVE`가 12비트로 잘려 0이 되던 버그를 즉시 찾았다.
> 깜빡임 코드만으로는 몇 시간 걸릴 문제였다.

**3단계 — 멀티미터 (40핀 커넥터에서)**

사각파에 멀티미터를 대면 **DC 평균 = 듀티비 × 3.3 V**가 찍힌다.

| 핀 | 듀티 | 기대 전압 |
|-|-:|-:|
| SDCLK | 50.0% | **1.65 V** |
| GDCLK | 92.5% | **3.05 V** |
| SDLE | 0.57% | **0.019 V** |
| SDCE0 | 8.98% | **0.296 V** |
| GDSP | 99.95% | **3.30 V** |
| GDOE / SDOE | 100% | **3.30 V** |
| SD[15:0] | 패턴 의존 | 0 V 도 3.3 V 도 아닌 중간값 |

**SD[15:8]이 0 V가 아닌지**가 16비트 전환이 실제로 먹혔다는 증거다.
8비트 모드에서는 상위 8비트가 상수 0이었다.

**고주사율 확인**

- 박스가 **초당 2.5회** 이동한다(24프레임 주기). 부드럽게 보이면 60 Hz가 나오는 것.
- 전면 전환이 **200 ms 안에** 끝나는지 본다. 일반 e-ink 컨트롤러가 같은 크기 패널에서
  1초 이상 걸리는 것과 비교하면 차이가 명확하다.
- LED D5(REFRESH_DONE 스트레치)가 프레임마다 깜빡인다.

### 7.5 디버그 가이드

| 증상 | 의심 지점 |
|-|-|
| 화면 아무 반응 없음 | `sys_ready`. UART가 한 줄도 안 나오면 스캔 자체가 안 도는 것이다(GDSP 무토글). BTN0 먼저 확인. `PMIC_READY`(D6)는 제거됐다 |
| 세로로 계속 밀림 / 굴러감 | GDCLK. `DEFAULT_HFP`를 키워 CKV low 펄스 확보 |
| 가로로 어긋남 / 찢어짐 | SDLE 타이밍, 또는 `DEFAULT_HACT`(320) 불일치 |
| 화면 일부만 나옴 | `DEFAULT_VACT`(1920) vs 실제 패널 라인 수 |
| 좌우 반전 | `csr_mirror_en` |
| 흐릿함 / 잔상 | `TEST_DRIVE_FRAMES` 조정 |
| 얼룩 / 가장자리 번짐 | VCOM 값, BORDER 핀 처리 |
| 노이즈 / 랜덤 픽셀 | 신호 무결성. 33Ω 직렬저항, 배선 길이, GND 리턴 |

### 7.6 튜닝 노브

첫 실행에서 만질 값은 사실상 두 개다.

| 노브 | 위치 | 현재 | 의미 |
|-|-|-|-|
| `TEST_DRIVE_FRAMES` | `caster.v` `PANEL_TEST` | 12 | 구동 시간 200 ms. 대비 부족하면 늘리고 과하면 줄임. 보통 8~20 |
| `DEFAULT_HFP` | `defines.vh` | 20 | GDCLK low 펄스 494 ns. 세로로 밀리면 40, 60으로 올림 |

`DEFAULT_HFP`는 **데이터시트 없이 정한 추정치**다. ED115OC1의 CKV 최소 low 펄스 규격을
구하면 그 값에 맞춘다. 키우는 만큼 프레임레이트는 조금 내려간다.

---

## 8. 영상 입력

Caster에는 스케일러가 없다. 프레임버퍼가 곧 입력 해상도이므로
**호스트 PC가 1280×960으로 출력해야 한다.**
다행히 이건 VESA 표준 모드라 커스텀 EDID가 필요 없다.

| 타이밍 | 픽셀 클럭 | 입력 레이트 | 비고 |
|-|-|-|-|
| 1280×960 @60Hz **CVT-RB** | ~85 MHz | 85 MP/s | 권장 — 한계 105 대비 19% 여유 |
| 1280×960 @60Hz VESA 표준 | 108 MHz | 108 MP/s | 한계 초과. RB로 지정할 것 |
| 내부 테스트 패턴 | — | — | 브링업 4단계까지 사용 |

**반드시 Reduced Blanking으로 지정한다.** VESA 표준 타이밍은 픽셀 클럭이 108 MHz라
직접 디시리얼라이저 한계를 넘긴다.

**현재 HDMI 경로는 레포에서 제거된 상태다.** EPD 출력 검증에 집중하기 위해
`rtl/dvi_rx.v`를 삭제하고, `top.v`의 DPI 스텁 배선과 CST의 HDMI 핀 정의도 함께 걷어냈다.
2단계에 착수할 때 `git checkout <제거 이전 커밋> -- rtl/dvi_rx.v`로 되살린 뒤
`top.v` 인스턴스화 + CST 핀 추가 + Makefile SRCS 등록을 하면 된다.

그때까지 픽셀 소스는 `vin.v`의 테스트 패턴 단독이다. `v_ready` 백프레셔를 존중하도록
이미 수정됐다 — 자유 주행하던 이전 버전은 기하가 어긋났다.

---

## 9. 파일별 수정 목록

### `rtl/defines.vh`

**반드시 고쳐야 하는 건 두 줄뿐이다.**

```verilog
`define OUTPUT_16B                   // 73번 줄 주석 해제 — 16비트 패널

`define DEFAULT_HACT   12'd320       // 640 -> 320
```

`DEFAULT_HACT`가 절반이 되는 이유는 16비트 + 2배 확대에서 1스텝이 4픽셀이 아니라 8픽셀을
담당하기 때문이다. 결과 폭은 320 × 8 = 2560으로 동일하다.
`DEFAULT_HACT`가 `INPUT_HACT`와 같은 320이 되는 것도 우연이 아니라,
SDCLK 1사이클이 코어 1워드에 대응한다는 [5.2](#52-2배-확대를-배선으로) 설계의 확인이다.

변경 없이 그대로 두는 값:

```verilog
`define INPUT_HACT     12'd320       // 1280 / 4
`define INPUT_VACT     12'd960
`define DEFAULT_VACT   12'd1920
`define DEFAULT_FBYTES `INPUT_HACT * 4 * `INPUT_VACT * 2   // 2,457,600 B
```

블랭킹은 현재 아래 값으로 적용돼 있다(htotal 344 / vtotal 1927 → 프레임당 662,888 clk).

```verilog
`define DEFAULT_HFP    8'd20      // GDCLK low 펄스 494 ns 확보 — 추정치, 실측 후 조정
`define DEFAULT_HSYNC  8'd2
`define DEFAULT_HBP    8'd2
`define DEFAULT_VFP    8'd4
`define DEFAULT_VSYNC  8'd1
`define DEFAULT_VBP    8'd2
```

`HFP`를 4까지 줄이면 htotal 328 / 632,056 clk가 되어 60 Hz에 37.9 MHz면 되지만,
세로 밀림이 나오면 오히려 키워야 하는 값이므로 여유 있는 쪽으로 잡아 뒀다
([3.2](#32-타이밍-파라미터), [7.6](#76-튜닝-노브)).

### `rtl/caster.v`

| 줄 | 현재 | 변경 |
|-|-|-|
| 408 | `s1_active`에 h·v 양쪽 게이팅 | 세로만 남김 — `h_cnt_offset[0]` 조건 제거 |
| 419 | `h_pixel >> 1` | 업스트림 복귀 (`h_pixel` 그대로) |
| 420 | `v_cnt_offset >> 1` | **유지** (세로는 여전히 2배) |
| 516-517 | `by_x_pos = scan_h_cnt[3:1]` | 업스트림 복귀 → `[2:0]`, `bn_x_pos` → `[3:0]` |
| 519, 526 | `by_y_pos = scan_v_cnt[3:1]` | **유지** (세로) |
| 540 | `line_reverse width = hact[10:1]` | 업스트림 복귀 → `hact[9:0]` |
| 786-810 | `OUTPUT_16B` 경로 (`out_clk = clk/2`) | [5.2](#52-2배-확대를-배선으로)의 2비트 복제 배선으로 교체 |
| 813 | `epd_sdclk = ~clk` | Gowin `ODDR` 프리미티브 |
| 821, 832 | `epd_gdoe = 1'b1`, `epd_sdoe = 1'b1` | `PWRGOOD` 전까지 Low 유지하도록 게이팅 |
| 824 | `epd_gdclk = scan_h_cnt[4]` | **업스트림 복귀 — 라인당 1펄스 + 1클럭 지연** |
| 291 | 무조건 `SCAN_WAITING` | `sys_ready && global_en && vin_vsync` 게이트 복원 |
| 342 | `frame_valid <= 1'b1` | FIFO 언더런 시 프레임 kill 복원 |

`caster.v:824`가 가장 심각하다. GDCLK(CKV)는 라인당 정확히 1펄스여야 게이트 드라이버가
한 행씩 내려가는데, `scan_h_cnt[4]`는 16클럭마다 토글하므로 라인당 약 21펄스가 나간다.
주석 자체가 "멀티미터로 1.65V DC가 찍히게 하려고"라고 적혀 있어 화면 구동용이 아니다.

### `rtl/sysclock.v`

전면 교체. Gowin rPLL 인스턴스 + 진짜 `lock` 신호 + 리셋 시퀀서.
현재의 `initial` + `#100` 방식은 합성 시 상수로 최적화된다.

### `rtl/mig_wrapper.v`

전면 교체. `DDR3_Memory_Interface_Top` 인스턴스 + [6.3](#63-memif-어댑터)의 shim.

### `rtl/bi_fifo.v` / `rtl/bo_fifo.v`

전면 교체. 그레이코드 비동기 FIFO.

### `rtl/top.v`

| 줄 | 현재 | 변경 |
|-|-|-|
| 157-160 | `dvi_de = 1'b0`, `dvi_pixel = 24'b0` | `dvi_rx` 인스턴스 연결 |
| 163 | `clk_epdc = clk_sys` | rPLL 출력 연결 |
| 424 | `sys_ready = 1'b1` | `PMIC_READY && ddr_calib_done` |
| 465 | `epdc_rst = 1'b0` | 리셋 시퀀서 출력 |
| 138-154 | 내부 60Hz vsync 생성기 | `dvi_rx`의 vsync로 대체 (브링업 중에는 유지) |

### `rtl/vin.v`

`v_ready` 백프레셔 존중하도록 수정, 또는 `dvi_rx` 연결 후 업스트림 구조로 복원.

### `constraints/gowin_constraints.cst`

193-209번 줄의 HDMI RX 핀 주석 해제. EPD 핀 배정은 변경 불필요.

### `xiao_pmic_controller/xiao_pmic_controller.ino`

#### 전압이 나오는 이유

현재 펌웨어로도 레일은 올라온다. 다만 **그건 I2C 때문이 아니다.**

TPS65185의 레일 기동은 **하드웨어 핀 `WAKEUP` → `PWRUP` 순서**로 이루어진다.
epdiy의 파워온 순서를 보면 명확하다(`epd_board_v7.c`).

```c
config_reg.pwrup = true;        // 1. WAKEUP/PWRUP 핀 assert  <- 레일은 여기서 올라온다
epd_board_set_ctrl(state, &mask);
while (!(pca9555_read_input(...) & CFG_PIN_PWRGOOD)) { }   // 2. PWRGOOD 핀 대기

tps_write_register(tps, TPS_REG_ENABLE, 0x3F);             // 3. 그 다음에 I2C
tps_set_vcom(tps, vcom);
while (!((tps_read_register(tps, TPS_REG_PG) & 0xFA) == 0xFA)) { }
```

EE03에서 `WAKEUP`/`PWRUP`은 IT8951이 쥐고 있고([1.4](#14-ee03-보드-실물-구조) 문제 2),
현재 펌웨어는 `xiao_pmic_controller.ino:53`에서 **`TFT_RST`를 HIGH로 놓아 IT8951을 기동시킨다.**
IT8951이 부팅하면서 `WAKEUP`/`PWRUP`을 구동하고, 그래서 레일이 올라온다.

`REG_ENABLE`(`0x01`) 주소는 맞으므로 어떤 레일을 켤지는 제대로 전달된다.
**즉 "전압이 나온다"는 관측은 정확하고, 그 공은 IT8951에 있다.**

> **순서가 중요하다.** 지금 되는 전원 시퀀스가 IT8951에 의존하므로,
> `WAKEUP`/`PWRUP` 2선을 XIAO로 먼저 끌어오기 전에 IT8951을 리셋에 붙잡으면
> **레일이 아예 안 올라온다.** 반드시 배선 → 펌웨어 → 리셋 홀드 순으로 진행한다.

#### 그래도 고쳐야 하는 것: 레지스터 주소

레일이 올라오는 것과 **올바른 전압이 나오는 것은 다른 문제다.**
레포 안의 `epdiy-main/src/board/tps65185.h`가 정확한 맵을 갖고 있다.

| 상수 | 현재 값 | 실제 TPS65185 레지스터 | 결과 |
|-|-|-|-|
| `REG_ENABLE` | `0x01` | `0x01` ENABLE | 맞음 |
| `REG_VCOM` | `0x00` | `0x00`은 **TMST_VALUE (읽기 전용 온도)** | 쓰기가 **무시**된다 → VCOM 미설정 |
| `REG_UP_SEQ` | `0x02` | `0x02`는 **VADJ (VPOS/VNEG 전압 설정)** | VPOS/VNEG 설정을 **의도치 않게 변경** |

따라서 실제 패널에 걸리는 전압은 코드가 주장하는 값이 아니다.

- **VCOM**: `0x00`에 쓴 값이 무시되므로 코드의 −1.31V는 반영되지 않는다.
  실제 값은 IT8951이 설정한 값이거나 PMIC 기본값이다.
- **VPOS/VNEG**: `0x02`(VADJ)에 `0x80`을 기록하면서 VSET 필드가 바뀐다.
  기본값 ±15V가 아닐 수 있다.
- **UPSEQ**: 실제 UPSEQ는 `0x09`/`0x0A`이므로 시퀀서 설정은 한 번도 건드려지지 않았다
  (기본 시퀀스로 동작 중).

> **먼저 할 일은 실측이다.** 멀티미터로 VCOM, VPOS, VNEG를 재서
> 코드가 주장하는 값과 얼마나 다른지 확인한다. 계조 품질과 잔상에 직접 영향을 준다.

**VCOM 계산식도 다르다.****VCOM 계산식도 다르다.** 현재 코드는 `VCOM = -(600 + val*10) mV`로 `val = 71`을 쓰는데,
epdiy의 실제 구현은 다음과 같다.

```c
void tps_set_vcom(dev, unsigned vcom_mV) {
    unsigned val = vcom_mV / 10;
    tps_write_register(dev, 4, (val & 0x100) >> 8);   // VCOM2: bit 8
    tps_write_register(dev, 3, val & 0xFF);           // VCOM1: bit 7..0
}
```

−1.31V를 원하면 `val = 1310 / 10 = 131`이므로 `0x04 = 0x00`, `0x03 = 0x83`이다.

**PWR_GOOD를 확인하지 않는다.** `pmic_read_reg()`가 정의만 되고 한 번도 호출되지 않는다.
`PMIC_READY`(D1)는 `setup()` 48번 줄에서 부팅 직후 무조건 HIGH로 올라가고
`loop()` 81번 줄에서 계속 HIGH로 유지된다. **핸드셰이크가 거짓 신호다.**
epdiy는 `TPS_REG_PG`(0x0F)를 마스크 `0xFA`로 폴링한다.

```c
while (!((tps_read_register(tps, TPS_REG_PG) & 0xFA) == 0xFA)) { /* 대기 */ }
```

**수정 방향**

```c
#define REG_ENABLE   0x01
#define REG_VADJ     0x02
#define REG_VCOM1    0x03
#define REG_VCOM2    0x04
#define REG_INT1     0x07
#define REG_UPSEQ0   0x09
#define REG_UPSEQ1   0x0A
#define REG_PG       0x0F

void pmic_power_up(unsigned vcom_mV) {
  digitalWrite(PIN_FPGA_READY, LOW);        // 레일 올라가기 전엔 반드시 LOW

  unsigned val = vcom_mV / 10;              // -1.31V -> 131
  pmic_write_reg(REG_VCOM2, (val >> 8) & 1);
  pmic_write_reg(REG_VCOM1, val & 0xFF);

  pmic_write_reg(REG_ENABLE, 0x3F);

  unsigned long t0 = millis();
  while ((pmic_read_reg(REG_PG) & 0xFA) != 0xFA) {
    if (millis() - t0 > 1000) { /* 실패 처리, READY 유지 LOW */ return; }
  }
  digitalWrite(PIN_FPGA_READY, HIGH);       // PG 확인 후에만 어서션
}
```

**그 외**

- **2초 keepalive를 제거한다**(`loop()` 84-91번 줄). `REG_ENABLE`/`REG_UP_SEQ`를 주기적으로
  다시 쓰는 것은 결함을 가리는 우회책이다. 대신 `REG_INT1`을 읽어 결함을 보고한다.
- **`REFRESH_DONE`(D2)이 읽히지 않는다.** `pinMode(INPUT)`만 하고 어디서도 쓰지 않는다.
  전원 차단 전에 이 신호로 스캔 종료를 기다리도록 연결한다.
- **IT8951 리셋을 해제하지 말 것.** 52-53번 줄이 `TFT_RST`를 HIGH로 놓아 TCON을 기동시킨다.
  전원만 인출하는 배선이라 버스 경합은 없지만, IT8951이 살아 있으면 PMIC에 마스터가 둘이 된다.
  `TFT_RST`를 **LOW로 유지**하고 XIAO가 단독 제어한다.
- **`WAKEUP`/`PWRUP` 직접 제어가 필요하다.** [1.4](#14-ee03-보드-실물-구조) 문제 2 참조.
  이 두 핀을 XIAO GPIO로 끌어오고, `pmic_power_up()` 맨 앞에서 `WAKEUP` HIGH → 대기 →
  `PWRUP` 펄스 순으로 구동한다.

### 웨이브폼

ED115OC1은 V220 Pearl 세대다. 벤더 `.wbf`를 구해 변환한다.

```
utils/wbf_waveform_dump  input.wbf  ed115oc1        # wbf -> IWF
Caster/utils/caster_wvfm_asm  ed115oc1.iwf  default_waveform.mem
```

구할 수 없으면 `Caster/waveform/test_M*_T0.mem`을 출발점으로 쓴다.
같은 V220 세대 웨이브폼이면 대체로 인식 가능한 화면은 나온다.
실제 프레임레이트가 웨이브폼 전제(85 Hz)와 다르면 프레임 수를 스케일링한다.

---

## 10. 마일스톤

두 단계로 나눈다. **1단계는 DDR3 없이 끝난다**([7장](#7-ddr3-없이-구동하기--1단계-목표)).

### 1단계 — 데모 영상으로 패널 구동

#### 1-1. SDCLK 최대값 실측

핀맵과 버스폭은 확정됐다. 남은 것은 SDCLK 최대, 블랭킹 규정값, VCOM 권장값,
FPC 삽입 방향(A/B) 육안 확인. **데이터시트는 조사 결과 공개된 것이 없다**
([남은 미확정 사항](#남은-미확정-사항)) — 낮은 클럭부터 올려가며 화면이 깨지는 지점을 찾는다.

`sysclock.v`의 rPLL 분주비만 바꿔 비트스트림을 여러 개 굽는다. 낮은 쪽부터:

| 목표 SDCLK | rPLL (27 MHz 입력) | 가능 프레임레이트 |
|-|-|-|
| 13.5 MHz | ×1/2 | 20.4 Hz |
| 20.25 MHz | ×3/4 | 30.5 Hz |
| 27 MHz | ×1 (바이패스) | 40.7 Hz |
| 33.75 MHz | ×5/4 | 50.9 Hz |
| **40.5 MHz** | **×3/2 (현재)** | **61.1 Hz** |

필요 SDCLK = 662,888 × 목표 프레임레이트. 40.5 MHz가 불가해도
프레임레이트를 낮춰 타협 가능하므로 **프로젝트가 무산되는 시나리오는 아니다.**

> **게이트** — SDCLK 최대값이 40.5 MHz 이상인가. 미달이면 PLL 분주비를 낮춘다.

#### 1-2. PMIC 브링업

현재 상태에서 이미 레일은 올라온다(IT8951이 `WAKEUP`/`PWRUP`을 구동).

1. **실측 먼저** — VCOM, VPOS, VNEG 실제 전압. 코드가 주장하는 값과 다를 것이다
2. 레지스터 주소를 고치고 VCOM을 `0x03`/`0x04`로 설정한 뒤 재측정
3. `WAKEUP`/`PWRUP` 2선을 XIAO GPIO로 배선하고 XIAO가 구동하도록 수정
4. XIAO 단독으로 레일이 올라오는지 확인한 **뒤에야** `TFT_RST`를 LOW 고정
5. `REG_PG` 폴링을 넣어 레일 검증. **FPGA측 `PMIC_READY`는 제거됐으므로**
   신호를 되살릴지, XIAO가 자체 판단만 할지 먼저 정할 것

> **게이트** — `REG_PG` & 0xFA == 0xFA, 6개 레일 규정 전압 도달.

#### 1-3. FPGA 단독 점검 (패널 미연결)

`make && make program`으로 올리고 BTN0로 드라이브 게이트를 연 뒤 **UART 콘솔**을
본다(§7.4 2단계). 멀티미터 표 대조는 보조다.

> **게이트** — `E=+000000 P=1 F=0`.
>
> **✅ 2026-08-24 통과.** 나아가 멀티미터로는 불가능한 항목까지 확인됐다:
> 16비트 버스 실효(`U=ff`), Hi-Z 0건(`Z=0000`), 가로 2배 배선 무결(`D=0000`),
> 세로 2배 라인 쌍 960개 무결(`V=0000 L=03c0`), 클럭 절대 주파수 40.472 MHz
> (59.96 fps 역산, 편차 −0.07%), 1200프레임 연속 동일값.
>
> 남은 구멍은 **픽셀 값 자체**다. 위 검사는 전부 버스의 구조적 성질만 본다.

#### 1-4. 패널 데모 점등

`make demo`. 패널을 연결하고 PMIC를 켠다.

> **게이트** — 화면 전체가 흑↔백으로 전환되고 흰 박스가 초당 2.5회 이동한다.
> **배선 · 전원 · 타이밍 · 프레임레이트가 전부 증명된다.**

미사용 가장자리(우 200 px · 하 150줄)는 전자잉크가 쌍안정이라 이전 상태를 유지한다.
전면 백색을 한 번 흘려 정리한다.

### 2단계 — PC 화면을 받는 모니터

#### 2-1. 툴체인 확인

Gowin DDR3 IP는 암호화 IP + DQS 하드블록이라 **현재 yosys/nextpnr/apicula 흐름으로는
빌드가 안 될 가능성이 높다.** IP 생성도 Gowin EDA가 필요하다.
shim을 다 써놓고 못 빌드하는 상황을 막으려면 이걸 먼저 확인한다.

> **게이트** — Gowin EDA에서 `glider_tang.gprj`가 빌드되는지 확인.

#### 2-2. DDR3 + 클럭 도메인 분리

`DDR3_Memory_Interface_Top`을 `mig_wrapper`에 넣고 shim을 작성한다.
`clk_mif`를 IP의 `clk_out`(100 MHz)에 물려 `clk_epdc`(40.5 MHz)와 분리한다.
이 시점에 이미 교체해둔 비동기 FIFO가 실제로 일을 시작한다.
주소 단위를 write → read-back으로 검증한다.

> **게이트** — `init_calib_complete` assert, 언더런 없이 연속 스캔, 계조 표시 확인.

#### 2-3. 영상 입력 체인

`dvi_rx`를 `top.v`에 연결하고 CST의 HDMI 핀을 활성화한다.
**`vin.v`에 RGB→Y 변환 · 4픽셀 패킹 · `clk_vi`→`clk_epdc` FIFO를 새로 만들어야 한다**
([부록 C](#부록-c-caster-기능-이식-감사)). 호스트를 1280×960 @60Hz CVT-RB로 설정한다.

이 시점에 **스캔 타이밍을 실제 입력 타이밍에서 재유도한다**([3.4](#34-스캔-타이밍과-입력-타이밍의-관계)).
`top.v:138-154`의 내부 vsync 생성기를 제거하고 `dvi_rx`의 vsync에 물린 뒤,
아래 두 조건을 반드시 확인한다.

- **EPDC 라인레이트가 입력 라인레이트의 2배보다 약간 느릴 것.** 빠르면 입력 FIFO가
  언더런한다. 현재 값은 118.3 kHz 대비 117.7 kHz로 0.5% 느려 안전 방향이다.
- **`vi_fifo`가 최소 1라인(320워드)을 버퍼링할 것.** EPDC는 320워드를 7.9 µs에 소비하는데
  입력은 15.0 µs에 걸쳐 보낸다.

> **게이트** — PC 화면이 패널에 뜬다. 프레임 kill(FIFO 언더런) 카운터가 0을 유지한다.

#### 2-4. 호스트 제어 (선택)

영역별 업데이트 · 모드 전환을 쓰려면 SPI CSR에 마스터를 붙여야 한다.
XIAO를 PMIC 겸 CSR 마스터로 쓰는 것이 가장 간단하다.

---

## 11. 리스크

| 리스크 | 영향 | 대응 |
|-|-|-|
| 패널 SDCLK 최대가 40.5 MHz 미만 | 높음 — 프레임레이트 천장이 직접 낮아짐 | **데이터시트 공개본 없음 확인(2026-08-22).** epdiy의 "max 10 MHz" 주장은 ESP32 처리량 한계로 판단되나 반증은 아님. 1-1에서 클럭 사다리로 최우선 실측. 미달 시 50 Hz→30 Hz로 후퇴 |
| PMIC `WAKEUP`/`PWRUP`이 IT8951 GPIO 뒤에 있음 | 높음 — IT8951 리셋 시 두 핀이 Hi-Z가 되어 PMIC 기동 불가 | 회로도 육안 확인 후 두 핀을 XIAO GPIO로 직접 배선 |
| IT8951을 살려둘 경우 PMIC 이중 마스터 | 중간 — 원인 불명의 전원 차단, VCOM 변동 | IT8951을 리셋에 붙잡고 XIAO 단독 제어 |
| EE03의 ±15V 전류 용량 부족 | 중간 — 대면적 전환 시 레일 드룹, 계조 불량 | 전면 흑↔백 전환에서 VPOS/VNEG 드룹 측정. 부족하면 외부 EPD 전원으로 교체 |
| XIAO 펌웨어의 PMIC 레지스터 주소 오류 | 높음 — VCOM 미설정, VADJ 오기록 | [9장](#9-파일별-수정-목록)대로 수정 후 PG 폴링으로 검증 |
| FPC 1번 핀 방향 오인 (40P-A/B) | 높음 — 전 신호 역순 연결, 패널 손상 가능 | 배선 전 1번 표식 육안 확인, 전원 인가 전 도통 시험 |
| EPDC 라인레이트가 입력 라인레이트 × 2보다 빠름 | 높음 — 입력 FIFO 언더런, 프레임 kill | 2-3에서 재유도. 반드시 "약간 느린 쪽"으로 맞춘다 ([3.4](#34-스캔-타이밍과-입력-타이밍의-관계)) |
| DDR3 주소 단위 불일치 | 중간 — 프레임버퍼가 엉킴 | 5단계에서 write → read-back 검증 |
| PMOD 점퍼 배선의 신호 무결성 | 중간 — 40.5 MHz에서 스큐, GND 리턴 3핀뿐 | 33Ω 직렬 저항, 짧은 리본 + GND 리턴, 가능하면 변환 PCB 제작 |
| GW2A-18 리소스 부족 | 중간 | 현재 합성 3209셀. DDR3 IP + 비동기 FIFO + `dvi_rx` 추가분을 4단계 후 재측정 |
| 웨이브폼 프레임레이트 불일치 | 중간 — 계조·대비 저하, 잔상 | 실제 프레임레이트에 맞춰 프레임 수 스케일링. V220 세대 대체 웨이브폼으로 시작 |
| 유효 해상도 150 PPI, 패널 86% 사용 | 낮음 — 설계상 감수한 대가 | 회피 불가. 네이티브가 필요하면 DDR3 채널 2개 이상인 보드로 이전 |

---

## 부록 A: 40핀 핀맵 복원 근거

데이터시트 없이 `pcb/40p-adapter-ab/adapter.kicad_sch`에서 복원했다.
이 어댑터는 40핀 패널 커넥터(J1, `AFC24-S40FIA-00`)를 Glider 마더보드의
50핀 커넥터(J2, `FPC-05F-50PH20`)로 변환한다.
스키매틱의 심볼 핀 좌표 → 와이어 그래프 → 라벨 연결성을 파싱해 넷리스트를 재구성했다.

루트 README가 ED115OC1을 **40P-A 어댑터, 검증 완료(Tested: Yes)** 로 분류하므로
이 배열이 그대로 적용된다.

**신뢰도 — 세 가지가 맞물린다.**

1. **양쪽 커넥터가 1:1로 완전히 대응한다.** 40핀의 `ED0`~`ED15`가 50핀의 `PD0`~`PD15`에,
   `GDCLK`/`GDSP`/`GDOE`/`SDCLK`/`SDLE`/`SDOE`/`SDCE0`가
   `PGDCLK`/`PGDSP`/`PGDOE`/`PSDCLK`/`PSDLE`/`PSDOE`/`PSDCE0`에 누락·중복 없이 이어진다.
   중간에 33Ω 직렬 저항 6팩이 들어가는 것까지 확인된다.
2. **핀 수가 정확히 떨어진다.** 데이터 16 + 제어 7 + 전원 7 + BORDER 1 + GND 3 + NC 6 = 40.
3. **epdiy와 일치한다.** 같은 40P-A 계열인 ED078KC1이 `displays.c`에서 `bus_width = 16`이다.

참고로 `epdiy-main`과 `utils/`에는 핀맵 정보가 없다. epdiy는 자체 보드 커넥터를 쓰고,
`utils/`는 웨이브폼 변환·플래시 도구뿐이다. PCB가 유일한 출처였다.

---

## 부록 B: 계산 근거

### 픽셀당 VRAM 트래픽

Caster는 픽셀당 16비트 상태를 저장한다(2세트 이전 픽셀값 + 픽셀별 타이머 카운터).
상태는 매 프레임 갱신되므로 픽셀당 2바이트 읽기 + 2바이트 쓰기가 필요하고,
여기에 입력 이미지 읽기 0.5바이트가 더해져 **4.5 B/px**다. 루트 README의 설명과 같다.

### 프레임당 클럭

```
스캔 스텝/라인 = 입력 가로 / 4 = 1280 / 4 = 320
htotal = 320 + HFP 20 + HSYNC 2 + HBP 2 = 344
vtotal = 1920 + VFP  4 + VSYNC 1 + VBP 2 = 1927
프레임당 스캔 = 344 x 1927 = 662,888 clk

60 Hz 필요 최소 SDCLK = 662,888 x 60 = 39.77 MHz
실제 코어 = rPLL 27 x 3/2 = 40.5 MHz          (여유 1.8%)
40.5 MHz에서의 스캔 한계 = 40.5e6 / 662,888 = 61.1 Hz
```

참고로 `HFP`를 4로 줄이면 htotal 328 / 632,056 clk / 37.9 MHz가 되지만,
`HFP` 20은 GDCLK low 펄스 494 ns 확보용 의도된 값이다([3.2](#32-타이밍-파라미터)).

### vsync 주기와 스캔 구간의 차이

```
vsync 주기 (top.v:162)  = 674,999 + 1 = 675,000 clk -> 40.5 MHz / 675,000 = 60.00 Hz
프레임당 스캔            = 662,888 clk               -> 16.37 ms
여유                     =  12,112 clk (1.8%)        ->  0.30 ms

입력 라인레이트 (CVT-RB) = 85.2 MHz / 1440   =  59.17 kHz
필요 EPDC 라인레이트     = x 2 (세로 2배 확대) = 118.3  kHz
실제 EPDC 라인레이트     = 40.5 MHz / 344     = 117.7  kHz   -> 0.5% 느림 (안전 방향)
```

여유가 존재하는 것 자체가 정상이라는 근거는 [3.4](#34-스캔-타이밍과-입력-타이밍의-관계)에 있다.

### DDR3 대역폭

```
프레임당 = 1280 x 960 x 4.5 B = 5,529,600 B
@ 60Hz   = 331.8 MB/s

Tang Primer 20K DDR3 = 16bit x 800 Mbps = 1600 MB/s 이론 피크
실효 70%             = 1120 MB/s
사용률               = 30%
```

### DVI 입력 레이트

```
CVT-RB 총 픽셀 = (1280 + 160) x (960 + 26) = 1440 x 986 = 1,419,840
@ 60Hz         = 85.2 MP/s
한계 (직접 디시리얼라이저) = 105 MP/s
```

### 128 배수 제약

```
memif.v: BURST_LENGTH = 16, BYTE_PER_WORD = 16
         BYTE_PER_CMD = 16 x 16 = 256 B

FBYTES = W x H x 2 가 256의 배수여야 함
      -> W x H 가 128의 배수여야 함

1280 x 960 = 1,228,800 = 9600 x 128   OK
```

### 패널 활용 범위

```
스캔 2560 x 1920 / 물리 2760 x 2070 = 면적 86.0%
미사용: 우측 200 px, 하단 150 줄
유효 화소 밀도: 300 PPI / 2 = 150 PPI
```

---

## 부록 C: Caster 기능 이식 감사

루트 README의 Features 목록을 기준으로 포트 상태를 확인한 결과다.

### 온전히 이식된 것 — EPDC 코어

포트 `caster.v`가 인스턴스하는 모듈이 업스트림과 **완전히 동일**하다.

| 모듈 | 역할 |
|-|-|
| `csr` | SPI 레지스터, 영역 업데이트 / 모드 전환 명령 |
| `mu_ram_2rw` | OSD 오버레이 RAM |
| `line_reverse` | 미러링 |
| `degamma` | 감마 보정 |
| `blue_noise_dithering` x2 | 블루노이즈 디더링 |
| `bayer_dithering` | 베이어 디더링 |
| `wvfmlut` x2 | 16계조 웨이브폼 룩업 |
| `pixel_processing` x4 | 픽셀 상태머신, 4 px/clk |

**8개 모드 전부 살아 있다** — `MANUAL_LUT`(디더 없음 / 블루노이즈),
`FAST_MONO`(디더 없음 / 베이어 / 블루노이즈), `FAST_GREY`,
`AUTO_LUT`(디더 없음 / 블루노이즈). 하이브리드 자동 계조 전환도 포함이다.

### 포팅하면서 바뀐 것

| 파일 | 변경 | 평가 |
|-|-|-|
| `csr.v` | `CSR_SELFBOOT` 조건부 분기 제거 → 항상 셀프부트 | 브링업엔 편하나, 호스트가 붙으면 초기화 시퀀스 재정리 필요 |
| `wvfmlut.v` | `INITIALIZE(0)` → `INITIALIZE(1)` | **올바른 조치.** Xilinx는 비트스트림에 초기값을 심지만 Gowin/apicula 흐름은 명시적 초기화가 필요 |

### 누락된 5개 파일의 실체

`error_diffusion_dithering.v`, `error_diffusion_kernel.v`, `clamp_signed.v`,
`linear_4b_quantizer.v`, `rgb2y.v`가 포트에 없다. 확인 결과:

- 앞의 4개는 **업스트림 `caster.v`도 인스턴스하지 않는다.** 상류에서 이미 연결이 끊긴
  코드다. 루트 README가 error-diffusion 디더링을 기능으로 내세우지만 `caster.v` 경로에는
  없다 — **포트의 회귀가 아니라 Caster 자체의 상태**다.
- `rgb2y.v`만 실제로 쓰인다. 위치는 `spartan6/vin_dvi.v`, `spartan6/vin_colormixer.v` —
  **영상 입력의 RGB→Y 변환**이다. 이것이 진짜 갭이다.

### 모니터로서 빠진 것

**1. 영상 입력 체인 전체.** 업스트림 `vin.v`는 `vin_fpdlink` + `vin_dpi` +
`vin_colormixer`를 물고 RGB→Y 변환 · 픽셀 패킹 · 클럭 도메인 크로싱을 한다.
포트의 `vin.v`는 하위 모듈이 없고 픽셀 포맷이 틀리다:

```verilog
assign v_pixel = dpi_de ? {14'b0, dpi_pixel} : test_pixel;
```

`v_pixel`은 **4픽셀 x 8비트 Y**여야 하는데 18비트 RGB를 0으로 확장해 넣고 있다.
`dvi_rx.v`를 연결해도 이대로는 화면이 안 나온다. 필요한 것은
RGB→Y 변환, 1 px/clk → 4 px/word 패킹, `clk_vi`→`clk_epdc` 비동기 FIFO(`vi_fifo`)다.

**2. 호스트 제어 경로.** SPI CSR은 살아 있으나 반대편에 마스터가 없다.
원본은 STM32H750이 USB HID로 PC와 통신하며 명령을 전달한다.

**3. DDR3.** [6장](#6-ddr3-서브시스템) 참조.

### 정리

| 루트 README 기능 | 포트 상태 |
|-|-|
| 병렬 I/F EPD 구동 | O (16비트 전환 완료) |
| 2치 / 4계조 / 16계조 모드 | O (8개 모드 전부) |
| 저지연 binary·4계조 구동 | O (로직 존재, DDR3 필요) |
| 하이브리드 자동 계조 | O (동상) |
| 베이어 · 블루노이즈 디더링 | O |
| error-diffusion 디더링 | X (업스트림도 미연결) |
| 영역별 업데이트 · 모드 전환 | 부분 — FPGA 쪽만, 호스트 없음 |
| 표준 HVsync 입력 | X (`dvi_rx` 미연결 + 픽셀 포맷 불일치) |
| < 20 us 지연 | DDR3 붙으면 성립 |

한 줄 요약 — **패널을 그리는 엔진은 100% 이식됐고, PC 화면을 받아 넣어주는 파이프가
통째로 없다.** 1단계 데모는 전자를 검증하고, 2단계가 후자를 만드는 작업이다.

---

## 참고

- 원본 Glider 설계: <https://github.com/zephray/Glider>
- Caster EPDC: <https://gitlab.com/zephray/Caster/>
- 루트 `README.md` — 전자잉크 기술 문서, 스크린 목록, 메모리·픽셀 레이트 한계표, TCON 파라미터 도출 절차
- `TangPrimer-20K-example-main/DDR-test/` — Gowin DDR3 IP 참조 예제
- `epdiy-main/src/displays.c` — 패널별 버스폭·클럭 참조값

### ED115OC1 데이터시트 조사 (2026-08-22, 웹)

- [Modos-Labs/Glider README — 패널 DB](https://github.com/Modos-Labs/Glider/blob/main/README.md) — 로컬 `README.md:1349`의 원본
- [epdiy V7 (Tindie) — "max 10 MHz clock" 주장 원문](https://www.tindie.com/products/fasani/epdiy-v7-816-bit-parallel-epaper-controller/)
- [epdiy Discussion #390 — ED115OC1 실제 테스트 "works"](https://github.com/vroland/epdiy/discussions/390)
- [epdiy Issue #391 — PSRAM 프레임버퍼 2 MB 한계](https://github.com/vroland/epdiy/issues/391)
- [Andesource ED115OC1 — "Data sheet: Work in progress"](https://www.andeelectronics.com/productdetail/ED115OC1.html)
- [PanelLook ED115OC1 개요 (캡차 차단)](https://www.panelook.com/ED115OC1_E_Ink_11.5_EPD_overview_68751.html)
